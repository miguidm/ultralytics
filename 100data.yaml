# YOLO dataset configuration

path: /datasets/ # base path to dataset

train: /datasets/train/images
val: /datasets/val/images

nc: 6 # number of classes
names: ["car", "motorcycle", "tricycle", "bus", "van", "truck"] # class names
